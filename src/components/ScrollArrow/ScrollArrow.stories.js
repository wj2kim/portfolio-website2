import React from "react";
import ScrollArrow from "./ScrollArrow";

export default {
  title: "Portfolio/ScrollArrow",
  component: ScrollArrow,
};

const Template = () => <ScrollArrow />;
export const basic = Template.bind({});
