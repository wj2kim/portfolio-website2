/** welcome section */
export type nameType = string;

export type DetailedType = {
  koreanName: string;
  englishName: string;
  location: string;
  red: string;
  blue: string;
  birthDay: string;
};
