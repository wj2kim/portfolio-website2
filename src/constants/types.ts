/** welcome section */
export type nameType = string;

export type DetailedType = {
  koreanName: string;
  englishName: string;
  location: string;
  red: string;
  blue: string;
  birthDay: string;
};

export type ProjectType = {
  title: string;
  description: string;
  image: string;
  tags: string[];
  source: string;
  code: string;
  id: number;
};
